<div class="sign-in__box box">
  <h1>Instate</h1>
  <div class="content">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), :html => {:class => 'sign-up__form'}) do |f| %>
        <%= devise_error_messages! %>

        <div class="field">
          <%= f.file_field :avatar %>
        </div>

        <div class="field">
          <%= f.email_field :email, autofocus: true, placeholder: '이메일', class: 'input' %>
        </div>

        <div class="field">
          <%= f.text_field :identify, autofocus: true, placeholder: '아이디', class: 'input' %>
        </div>

        <div class="field">
          <%= f.text_field :name, autofocus: true, placeholder: '성명', class: 'input' %>
        </div>

        <div class="field">
          <%= f.password_field :password, autocomplete: "off", placeholder: '비밀번호', class: 'input' %>
        </div>

        <div class="field">
          <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: '비밀번호 확인', class: 'input' %>

          <% if @minimum_password_length %>
              <p class="alert__message">(비밀번호는 최소 <%= @minimum_password_length %>글자 이상으로 입력해주세요.)</p>
          <% end %>
        </div>

        <div class="field">
          <%= f.submit "가입", class: 'button-blue' %>
        </div>

        <div class="field">
          <p class="information__message">
            가입하면 Instate의 <b>약관</b>과 <b>개인정보취급방침</b>에 동의하게됩니다.
          </p>
        </div>
    <% end %>
  </div>
</div>

<%- if controller_name != 'sessions' %>
    <div class="sign-in__box box">
      <p class="register__message">
        계정이 있으신가요? <%= link_to "로그인", new_session_path(resource_name) %><br/>
      </p>
    </div>
<% end -%>
